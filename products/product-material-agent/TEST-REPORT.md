# 1.0.0 测试报告

测试日期：2026-08-26  
测试对象：`product-material-agent` 源包

## RED 基线

修改前执行：

```text
node scripts/check-skill-package.mjs products/product-material-agent
```

实际结果为失败，首个错误是缺少 `README.md`；同时确认本任务要求的商业根文件、合同用例和公开示例对均不存在。该失败由缺少商业包合同触发，不是命令或语法错误。

## 验证口径

最终验证必须覆盖：JSON 解析、必需文件、四级证据、五种工作流状态、六项固定输出、审核责任、无凭证、无自动发布和公开合成示例一致性。

## GREEN 源码验证

- `node --test tests/skill-package.test.mjs`：通过，7 项通过，0 项失败。
- Task 2 源包合同审计：通过；13 个新增文件非空，三个 JSON 文件可解析，清单字段、四级证据、五种状态、六项输出、合同案例和公开示例一致。
- 未完成标记与常见密钥特征扫描：通过。
- `node verify.mjs`：通过；校验23个包内文件和5个合同案例，运行依赖仅为 Node.js 标准库。
- 安装配置回归：通过；`config.json` 必须把事实、素材授权、品牌和发布决定四项示例责任人全部替换为实际姓名或可审计角色 ID，支持含空格姓名，缺失或保留示例值均以非零退出。

`CHECKSUMS.sha256` 已覆盖除自身和本地 `config.json` 外的全部发布文件。ZIP 文件大小、ZIP SHA-256、归档与源目录逐文件一致性由外部统一发布审计记录，避免在包内形成自引用哈希。
